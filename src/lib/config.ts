/**
 * Site-wide display config.
 *
 * RANDOM_ORDER — when true, notebooks and drawings are shuffled
 * per-visitor (seeded by the session_seed cookie). When false, they
 * display chronologically: notebooks in NOTEBOOKS array order,
 * drawings by display_order ascending.
 */
export const RANDOM_ORDER = true;
