/**
 * Site-wide display config.
 *
 * RANDOM_ORDER — when true, the drawings within a notebook gallery are
 * shuffled per-visitor (seeded by the session_seed cookie). When false,
 * they display by display_order ascending. (The /drawing notebook index
 * and the All Drawings view sort by the chronology key regardless.)
 */
export const RANDOM_ORDER = true;
